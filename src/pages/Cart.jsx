import React from'react';import{useNavigate}from'react-router-dom';import{AlertTriangle,ArrowRight}from'lucide-react';import{Button,Layout,Status}from'../components/Layout.jsx';const items=[['Paneer Kathi Roll','Kathi Junction','1','₹120','READY FOR PICKUP','green'],['Cold Coffee','Sweet Truth','1','₹90','PREPARING','amber']];export default function Cart(){const go=useNavigate();return <Layout><main className="page cart-page"><div className="page-title"><h1>Your cart</h1><p>Review your items before placing the order.</p></div><div className="warning"><AlertTriangle size={18}/><span>Orders are collected at the hostel gate selected during checkout.</span></div><h2 className="subheading">Cart items</h2><div className="table cart-table"><div className="table-row head"><span>Item</span><span>Shop</span><span>Qty</span><span>Price</span><span>Status</span></div>{items.map(([i,s,q,p,st,t])=><div className="table-row" key={i}><span>{i}</span><span>{s}</span><span>{q}</span><span>{p}</span><span><Status tone={t}>{st}</Status></span></div>)}</div><div className="cart-actions"><Button outline onClick={()=>go('/shops')}>Continue shopping</Button><div className="summary"><strong>Total ₹210</strong><Button onClick={()=>go('/orders')}>Proceed to checkout <ArrowRight size={14}/></Button></div></div></main></Layout>}
